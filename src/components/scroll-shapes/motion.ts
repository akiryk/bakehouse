import type { ScrollShapesConfig } from "./config";

/**
 * Initializes the scroll-shapes ambient layer.
 * Called once at page load by ScrollShapes.astro.
 */
export function initScrollShapes(
  _container: HTMLElement,
  _config: ScrollShapesConfig,
): void {
  // Stub — shape generation and ticker added in subsequent steps.
}
